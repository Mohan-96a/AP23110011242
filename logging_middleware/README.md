# Logging Middleware

This folder is intended for middleware components that handle logging for the notification application. You can add request/response logging, error logging, and analytics traces here.
