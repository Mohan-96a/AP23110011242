# Notification App Frontend

This frontend folder contains a minimal static app that consumes the backend notification API.

## Features

- Fetches notifications from backend API
- Displays top notifications by priority and timestamp
- Simple UX for campus notification inbox

## Run frontend

1. `cd notification_app_fe`
2. `npm install`
3. `npm start`

Open `http://localhost:3000` after the server starts.
